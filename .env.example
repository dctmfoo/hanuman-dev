# Copy to .env for local overrides. Do not commit secrets.

# Where run state/artifacts are stored. Defaults to ~/.halo
# Example: HALO_HOME=/path/to/halo
HALO_HOME=

# Engine selection. Defaults to Codex CLI.
# Set to "fake-codex" for tests/smoke runs.
HANUMAN_ENGINE=

# Fake Codex scenario for tests (ok | no-json)
HANUMAN_FAKE_CODEX_SCENARIO=

# Engine auth expectations:
# - The real executor uses the `codex` CLI from your PATH.
# - Authenticate Codex CLI via its own login/profile flow.
# - Do not place API keys in this file.
